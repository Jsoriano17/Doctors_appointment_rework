<%= form_for [@doctor, @appointment] do |f| %>
    <%= f.label :date %>
    <%= f.date_field :date, required: true %>

    <%= f.label :patient %>
    <%= f.select :patient_id, @doctor.patients.collect { |u| [u.name, u.id] } %>
    <%= f.submit%>
<% end %>